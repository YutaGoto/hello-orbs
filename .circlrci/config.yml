version: 2.1

orbs:
  hello-command: yutagoto/hello-world@dev:first

jobs:
  build:
    docker:
    - image: bash
    steps:
    - run: "echo 'testing building...'"
    - hello-command/hello:
        username: ".Gotch"


workflows:
  main:
    jobs:
    - build
